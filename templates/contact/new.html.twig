{% extends 'base.html.twig' %}

{% block title %}New Contact{% endblock %}

{% block body %}
    <h1>Create new Contact</h1>

    {{ include('contact/_form.html.twig') }}
	<br />
	<hr />
    <a href="{{ path('contact_index') }}">back to list</a><br /><br />
    <a href="{{ path('contacts_upload') }}">Upload file from spreadsheet</a>
{% endblock %}