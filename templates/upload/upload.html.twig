{% extends 'base.html.twig' %}
{% block body %}
<div id="page-wrapper">
{{ form_start(form, {'attr': {'class': '' }} ) }}
{# {{ dump(form) }}
 #}<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                <p>Upload from Excel (.xls) file. Format: Column A: Name, Column B: phone number. Example: /public/excelFiles/example.xlsx</p>
            </div>
            <div class="panel-body">
                <div class="row">
                    <div class="col-lg-6">
                     <div class="form-group">
                            <label>Select file</label>
					        {{ form_errors(form.file) }}
					        {{ form_row(form.file) }}
                        </div>
                        <div class="form-group">
						<button class="btn">{{ button_label|default('Save') }}</button>
                        </div>
                    </div>
                </div>
                <!-- /.row (nested) -->
            </div>
            <!-- /.panel-body -->
        </div>
        <!-- /.panel -->
    </div>
    <!-- /.col-lg-12 -->
</div>
{{ form_end(form) }}
</div>

{% endblock %}