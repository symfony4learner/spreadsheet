<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
	    {% for message in app.flashes('success') %}
	        {{message}}
	    {% endfor %}

	    {% for message in app.flashes('error') %}
			{{ message}}	    
		{% endfor %}        	
        <a href="{{ path('default') }}">Homepage</a>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
        	
    </body>
</html>
